import { Body, Controller, Delete, Get, Param, ParseIntPipe, Patch, Post } from '@nestjs/common'; import { CreateMedidorDto } from './dto/create-medidor.dto'; import { UpdateMedidorDto } from './dto/update-medidor.dto'; import { MedidoresService } from './medidores.service'; @Controller('medidores') export class MedidoresController{constructor(private service:MedidoresService){} @Get() findAll(){return this.service.findAll();} @Post() create(@Body() dto:CreateMedidorDto){return this.service.create(dto);} @Get(':id') findOne(@Param('id',ParseIntPipe) id:number){return this.service.findOne(id);} @Patch(':id') update(@Param('id',ParseIntPipe) id:number,@Body() dto:UpdateMedidorDto){return this.service.update(id,dto);} @Delete(':id') remove(@Param('id',ParseIntPipe) id:number){return this.service.remove(id);}}
