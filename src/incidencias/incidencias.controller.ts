import { Controller, Get, Param, ParseIntPipe, Patch } from '@nestjs/common'; import { IncidenciasService } from './incidencias.service'; @Controller('incidencias') export class IncidenciasController{constructor(private service:IncidenciasService){} @Get() findAll(){return this.service.findAll();} @Patch(':id/resolver') resolver(@Param('id',ParseIntPipe) id:number){return this.service.resolver(id);}}
